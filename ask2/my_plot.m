function [] = my_plot(x, xq)
    h = stem(1:length(xq), xq, 'linewidth', 2);
    %hold on
    %h = stem(1:length(centers), centers, 'linewidth', 2);
    %hold on
    %plot(1:length(x), x, 'linewidth', 2)
    %hold off
end